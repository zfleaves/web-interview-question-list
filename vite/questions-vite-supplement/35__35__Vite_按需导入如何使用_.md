# 35. Vite 按需导入如何使用？

**答案：**

```vue
<!-- 自动导入 Vue API -->
<template>
  <div>{{ count }}</div>
  <button @click="increment">Increment</button>
</template>

<script setup>
// 不需要手动导入 ref、computed 等
const count = ref(0);
const increment = () => {
  count.value++;
};
</script>
```

---