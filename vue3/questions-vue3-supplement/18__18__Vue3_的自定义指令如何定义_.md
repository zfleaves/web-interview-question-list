# 18. Vue3 的自定义指令如何定义？

**答案：**

### 基本用法

```javascript
// 全局指令
const app = createApp(App);

app.directive('focus', {
  mounted(el) {
    el.focus();
  }
});

// 局部指令
export default {
  directives: {
    focus: {
      mounted(el) {
        el.focus();
      }
    }
  }
};
```

---