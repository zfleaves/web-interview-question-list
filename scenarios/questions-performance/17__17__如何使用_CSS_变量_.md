# 17. 如何使用 CSS 变量？

**答案：**

CSS 变量可以提高代码复用性，便于维护。

```css
/* 定义变量 */
:root {
  --primary-color: #1890ff;
  --font-size-base: 14px;
}

/* 使用变量 */
.button {
  background: var(--primary-color);
  font-size: var(--font-size-base);
}
```

---